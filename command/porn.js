const Discord = require("discord.js");

module.exports.run = async (client, message, args, tools) => {
   
    if(cmd === `${prefix}hello`){
        return message.member.send("https://rule34.xxx/index.php?page=post&s=view&id=3312363");
  }
}